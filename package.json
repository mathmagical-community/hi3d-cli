{
  "name": "hi3d-cli-monorepo",
  "private": true,
  "version": "2.0.0",
  "description": "hi3d-cli — Hi3D image-to-3D CLI and MCP server for terminals and AI agents",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/ensure-constants.mjs && tsc -b packages/core packages/blender packages/mcp packages/cli",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo release",
    "test": "bash test/run-e2e.sh",
    "test:blender": "bash test/run-blender-e2e.sh",
    "release": "node scripts/build-release.mjs",
    "release:pack": "npm run release && cd release && npm pack ./hi3d-cli",
    "docs:wiki": "node scripts/gen-wiki-tools.mjs",
    "check-version": "node scripts/check-version.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.0"
  }
}
